<UserControl x:Class="win.views.computerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:win.views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="5,0,0,5">
            <TextBlock Text="{Binding User}" MinWidth="50" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding Cpu}" MinWidth="50" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding Mb}" MinWidth="50" Margin="0,0,5,0"/>
            <TextBlock Text="{Binding Ram}" MinWidth="50" Margin="0,0,5,0"/>
        </StackPanel> 
    </Grid>
</UserControl>
